# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database — async engine (asyncpg), migrations (alembic)
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Validation & settings
pydantic==2.7.1
pydantic[email]==2.7.1
pydantic-settings==2.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Redis async client
redis[hiredis]==5.0.4

# HTTP client (adapters)
httpx==0.27.0

# AI / Agent orchestration (P2 — included here for single requirements.txt)
langgraph==0.1.4
openai==1.30.1

# Utilities
python-dateutil==2.9.0
